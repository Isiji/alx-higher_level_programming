#!/usr/bin/python3
"""this functions compares if an instance is of the exact specified class"""
def is_same_class(obj, a_class):
    """returns true or false"""
    if type(obj == )a_class:
        return True
    return False
